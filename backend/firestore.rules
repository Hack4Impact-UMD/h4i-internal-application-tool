rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      // disabling writes everywhere for now until auth is setup
      allow write: if false;
    }
  }
}
